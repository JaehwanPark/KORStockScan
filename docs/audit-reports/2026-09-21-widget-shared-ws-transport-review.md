# Widget shared WS transport: first P0/P1 handoff

## Integrated source follow-up code review and repairs

- User requested repeated code review, repair and re-review after the integrated-source handoff. Reviewed the existing publisher projection, shared reader/comparison/census, three collector success/failure publishers, observation recorder and widget replay consumers. Scope is comparison evidence and local snapshot validation; `_AL` acceptance remains enabled for all symbols. No new source module, protocol request, FID mapping, REG/recovery flow, policy or execution selector is introduced. The current-session official reference recorded below remains applicable.
- Reproduced eight failing cases before repair: duplicate publication of one read within/across15-minute windows (2), cross-scope comparison hiding negative or over20-second clock separation (2), string/object/non-string-list registration evidence accepted by Python membership (3), and conflicting item/suffix metadata (1). [Before-fix reproduction](../../tmp/widget-ws-review-reproduction-20260921.log).
- Repairs: count each reader result only once per consumer process, including failure republishing after recorder/write exceptions; preserve distinct reads, fork isolation and failed denominators. Add `evaluation_count_basis=once_per_reader_result` so future consumers can distinguish the corrected counter. Check clock comparability before scope classification and preserve `same_market_data_scope` separately; a bad REST pairing does not revoke valid WS reception. Require the producer registration evidence to be an array of strings, without assuming all registered items are stock codes; verify each selected0B/0D suffix against its exact item. Real selected-route/epoch/clock and stale/crossed-book guards remain.
- Regression coverage also exercises the actual Samsung failure publisher and recorder, distinct equal-valued reads, unrelated registered market items, count-receipt immutability, process provenance failures, source switching and bounded history. Existing multi-evaluation fixtures now allocate distinct reader-result objects, as production does. No real broker/provider/account calls occur in these checks.
- Final direct-impact suite: **346 PASS**, one existing fork deprecation warning, covering quote/three collectors/publisher/widget paired-policy and mechanical replay. Python compile, Ruff F/E9 and diff whitespace PASS. [Final test log](../../tmp/widget-ws-review-scoped-final-20260921.log). Initial broader probe included main compact finalization:381 PASS/2 FAIL; both `compact_preopen_freeze_without_dated_policy` failures reproduce unchanged in immutable6c1bfa172 (2 FAIL/87 deselected). They are separate pre-existing main-policy time-fixture failures, not suppressed or repaired by loosening PREOPEN. [Baseline reproduction](../../tmp/widget-ws-review-baseline-failures-20260921.log).
- Re-review has no unresolved finding in this shared-WS comparison repair scope. Whole-repository GREEN, natural coverage or economic completion is not claimed. Worktree code is repaired; deployed6c1bfa172 collectors have not consumed this follow-up, and no commit/push/restart or P2/P3 activation was performed in this review. Preserve the active observation window. Prior v3 counters without the new count-basis marker require raw success/failure publication reconciliation before treating them as unique evaluations; this review does not retroactively certify those counters. Actual P2/P3 acceptance stays with the existing checklist owner.

## Integrated source deployment and continuation

- User requested implementation status review and continuation, with prior deployment/restart approval retained. Source commit `6c1bfa172` was committed and pushed; immutable release `/home/ubuntu/KORStockScan-runtime-releases/widget-ws-integrated-20260921-6c1bfa172` passed the same272 affected tests independently. Existing fork warning remains. The related consumer-fork census repair is included; unrelated scale-in/monitor tests and integrated-review document edits remain in the workspace, unstaged.
- At13:51 the existing three read-only collector pins were updated and services restarted: Samsung PID209903, Doosan209901, Hanwha Ocean209907. All active/running, Result success, NRestarts0; actual cwd and inherited source commit match the new immutable release. Existing10-second cadence and other unit environment remain. Main selector and WS producer PID174887/4b3b4080c/epoch1 are unchanged. Trader PID75952, episode executors and schedules are unchanged; Samsung recorded carry25 and all three widget order lists remain intact/empty. No broker account reconciliation is claimed.
- At13:51:56–57 all three actual consumers report comparison_v2/source_bound_v3, same-symbol `_AL`, `valid_ws_comparison_input`, correct consumer PID and producer generation. First6 evaluations each are valid WS, gap0; REST unobserved Samsung3, Doosan4, Hanwha0. Hanwha's latest REST snapshot is ok while Samsung/Doosan remain unavailable due to existing REST waits. This directly confirms the deployed acceptance correction, not input substitution or full-window availability. Natural scheduled health13:51:43 is PASS. [Deployment, backups, physical-release tests and consumer evidence](../../tmp/widget-ws-integrated-deploy-20260921/).
- Rollback restores only the three saved `*-before.conf` files to their original `zzzzzzzzzzzzzzzzzzzzzzzz-duplicate-read-20260921.conf` paths, daemon-reloads and restarts those three collectors. Existing other drop-ins, main selection, custody, policy and raw observations are not rollback targets. Source release remains retained. No new service, timer, subscription or main restart was introduced.
- **Current next natural acceptance:14:45 or later**, using complete14:00–14:15/14:15–14:30/14:30–14:45 windows from these consumer generations. The13:45–14:00 startup partial window is diagnostic only. Prior13:00–13:45 exact-request-only aggregates are historical; they do not certify the corrected consumer. Keep failed denominators and per-source counts, require valid field clocks/epoch/producer continuity, and retain registered-to-received-to-consumed evidence. Another generation change must be reflected in the evidence, not merged silently. Elapsed time alone does not pass acceptance.
- P2 remains unselected (`selected_input=existing_rest`). Dependency review found that all three collectors also use ka10001 `low_pric` for displayed day-low distance, and Samsung uses `flu_rt` in relative observations. The shared exact-route0B projection currently preserves trade price/quantity/aggressor but not those fields. P2 must preserve separately timed REST auxiliary values or add officially verified same-source fields before removing ka10001; silently substituting a five-field quote would lose existing output. Existing `_parse_bbo`/source-quality validation also distinguishes REST receipt metadata and must receive explicit WS provenance when selection is implemented. These are concrete next implementation dependencies, not authority to fake REST receipts or change entry guards.
- P3 remains source-blocked: coalesced EventBus updates and bounded tick tails do not prove continuous full-minute input. Keep existing completed REST bars; current acceptance correction does not establish WS bar equivalence. Expanded58/research collectors have not been repinned or switched. Existing single checklist owner retains P2/P3/P5; no duplicate task or background timer was created.

## Integrated symbol source acceptance correction

- User corrected the acceptance contract: valid same-symbol `_AL` reception must count for Samsung and all other symbols. The former reader used the REST request item as the only WS item and incorrectly rejected Samsung with `item_not_registered` despite valid integrated data. This section supersedes that exact-request-only requirement and cross-route REST equality as a transition gate; actual source route integrity remains mandatory.
- Fresh official reference retrieved `2026-09-21T04:43:22.625567+00:00`, upstream SHA `953e5dbff123f437ab4d11a78a95191a685eb51f`. Inspected `kiwoom/specs.py`, `kiwoom/core/ws_client.py`, `kiwoom/realtime/packets.py`, `kiwoom/realtime/schemas.py`, packaged `kiwoom/_data/kiwoom_api_spec.json`0B/0D/ka10001/ka10004 and PRD/MOCK Postman envelopes. `kiwoom_docs` remains absent. Official0B/0D item descriptions explicitly support KRX/raw, NXT/`_NX`, SOR/`_AL`; price/quantity FIDs10/41/51/61/71 retain existing semantics. [Reference receipt](../../tmp/widget-ws-integrated-reference-20260921/receipt.json). No broker request, subscription, authentication, wire parser or recovery change.
- Existing common reader now prefers registered same-symbol `_AL`, otherwise the original request item; it preserves both `request_code` and `ws_request_code`. A registered integrated source still needs complete fresh0B/0D, exact source item/route/epoch, valid producer generation, positive uncrossed book and current session/date. Partial or stale integrated input remains a visible source gap. It is never relabeled as a KRX or NXT quote. This applies generically, without a Samsung exception; future expanded consumers must reuse this reader, not copy its rule. Episode completed bars and order liquidity checks retain their own source contracts.
- Cross-route REST observations retain values, field differences and clocks, but use `different_market_data_scope`; they count as valid WS input, not unexplained same-route mismatch or false same-event equality. Transport schema is comparison_v2; census source_bound_v3 retains source-item counts inside each window, failed denominators, process/fork isolation and producer-generation flags. Existing receipt copies remain immutable after later counts change. Old exact-request failure aggregates cannot be retroactively relabeled PASS without their original source evidence.
- At13:45:42 KST the workspace reader consumed actual published data for005930_AL,034020_AL,042660_AL: all three `valid_ws_comparison_input`. [Read-only result](../../tmp/widget-ws-integrated-reader-20260921.json). This corrects the source-acceptance defect; it is not an immutable deployment/PID receipt or completed P2 source adoption. Live collectors remain on their selected releases and REST input. No service restart, order, holding/policy mutation or account/API probe occurred.
- Validation: initial affected reader/three-collector suite271 PASS, one existing fork deprecation warning. Regression cases cover four symbols across KRX/NXT/integrated request codes, actual source provenance, scope-aware comparison, census immutability and stale/mixed-item/wrong-symbol/partial/route/epoch rejection. Final affected suite272 PASS after adding exact-to-integrated source selection and retained-window denominator coverage. Compile, Ruff F/E9, diff whitespace, local links and print-only checklist parser PASS; the existing stable owner parses exactly once. Re-review found no unresolved defect in this comparison-reader change. Trading execution suites and report regeneration were not run because no execution path changed. Existing independent workspace changes are preserved.

## P2/P3 requested: prerequisite review and bounded census repair

- User requested P2/P3 progression after P0/P1. This authorizes staged work, not treating P5 as passed. At12:44 KST, the actual producer is fee046b21/PID164792 (12:39:38 restart), not the quoted87742bd0b/PID131444. Existing checklist owner requires complete12:45–13:00/13:00–13:15/13:15–13:30 windows, assessed after13:30. Time alone will not pass the gate; unexplained required-field differences and coverage gaps must also close.
- Latest12:44 snapshots retain compare_only/existing_rest. The mixed12:30–12:45 window has89 comparisons per basic consumer; valid WS Samsung28, Doosan86, Hanwha85; REST-unobserved72/39/40 and different observations17/48/38. These are incomplete, mixed-producer comparison counts, not accepted coverage or measured REST savings. Latest Samsung differs in both BBO quantities; Doosan differs in price and quantities. Clock separation can explain possible movement, but does not prove these individual differences safe for source substitution.
- Found a P2 evidence defect: census windows were keyed only by consumer date/item/session, while the enclosing producer metadata described only the latest read. A main-only restart/reconnect could silently mix generations. Extend the existing census, without resetting failure counters, with per-window producer process/commit/epoch, sticky mixed/missing flags and first/last observation clocks. Schema widget_transport_census_generation_bound_v2 distinguishes new evidence; old unbound windows are not retroactively certified. Keep four-window retention and existing comparison-only authority. No new producer, API, CLI or module is added.
- P3 source prerequisite remains open: shared checkpoint/tick-tail is not a continuous trade ledger. The inspected WS EventBus dispatch coalesces pending updates by symbol; strength_momentum_history is a bounded deque. Neither proves lossless full-minute input. Low-price gateway completed_sor_minute_bars still consumes same-route ka10080 completed bars through its existing same-minute cache and OHLC/duplicate/time checks. No synthetic WS bars, episode input substitution or source-quality relaxation is introduced. P3 still needs an eligible continuous source, epoch/revision/late-tick validation and same-bar/same-policy signal equivalence before adoption; adding only a cache is not P3 completion.
- Scope is local comparison-census metadata only: no Kiwoom request/parser/FID/subscription/recovery change, so no new official protocol gate mutation is invoked. Review follows the existing reader -> collector -> raw recorder -> paired-replay boundaries. First affected checks250 PASS; final validation below includes paired replay. P2 selection/subscription expansion and P3 bar integration are not implemented or deployed by this prerequisite repair. Main/collector/trader restarts are deliberately not performed during the running P1 comparison; rollout awaits its existing gate rather than resetting the observation period again.
- Final review/validation:294 affected reader/three-collector/paired-replay tests PASS, including PID reuse/start-tick/boot/commit/connection-epoch changes, initially missing producer, retained failed denominator and bounded history. Existing fork warning remains. Ruff F/E9, compile, diff whitespace and print-only checklist parser PASS. No unresolved finding in this census-only repair; P2/P3 prerequisites remain OPEN. Code is prepared for the next authorized immutable handoff, not proof of live v2 census consumption.

## Scope and authority

User authorized implementation, repeated review/fixes, commit/push, deployment and startup of the [staged proposal](../proposals/widget-episode-shared-ws-market-data-improvement-plan-2026-09-21.md). Its former document-only statement is historical, not a veto of this approval. First handoff is P0/P1/P4; P2 input selection/58-symbol and research-watch expansion requires P5 comparison acceptance. P3 completed-bar/episode changes are explicitly later work. No extra daemon, database, CLI, policy, subscription, token lifecycle, account/order request or live source selection is introduced here.

Location gate: one small common reader/comparison module belongs in `src/trading/market/shared_ws_snapshot.py`. Existing `market_data_cache` is in-process quote storage, not an on-disk consumer; putting file/process validation there would mix contracts. Existing publisher, three advisory collectors/recorder and tests are extended; no engine-root module is added.

## Official gate and P0 evidence

- Upstream main freshly resolved at `2026-09-21T02:22:43.295586+00:00` to `953e5dbff123f437ab4d11a78a95191a685eb51f`. Inspected complete `kiwoom/realtime/packets.py`, `kiwoom/realtime/schemas.py`, `kiwoom/core/ws_client.py`; packaged `kiwoom/_data/kiwoom_api_spec.json` request/selected response fields for0B/0D/ka10001/ka10004; PRD/MOCK requests in `postman/kiwoom-openapi.postman_collection.json`. Tree confirms no `kiwoom_docs`. Same-session specs/core REST checks retain their unchanged protocol relevance; no transport envelope/parser/FID/REG/REMOVE code is modified.
- Official WS endpoint is `/api/dostk/websocket` on real/demo port10000; LOGIN/PING echo and REG refresh1 retain-existing semantics remain unchanged. The local cap56 from main PID117559 is not an official broker maximum. No additional subscriptions are attempted; expanded cohort capacity remains unverified.
- Read-only current checkpoint: roughly3.5MB, 30 stock rows initially; subsequent snapshot40 exact routes/36 recent0B+0D pairs is a receive census, **not** broker registration acceptance or all-owner capacity. Three actual widget symbols are Samsung005930, Doosan Enerbility034020 and Hanwha Ocean042660. All three currently have their required KRX route; Samsung also has NXT/integrated observations. Doosan000150 is a different symbol, not this widget's input gap.
- Base main release `0d7fed630`, PID117559; three read-only collectors are still pinned to `ffd565c83` (`entry-source-clock-scope-r3-20260917`), PIDs682/665/666. Those three collector source files have no source diff from the pre-change main HEAD; user custody/strategy state is not migrated or rewritten.

| Consumer field | Existing REST | Official WS field | Constraint |
| --- | --- | --- | --- |
| current price | ka10001 cur_prc | 0B FID10 | KRW, signed protocol value normalized by existing parser |
| best ask/bid | ka10004 sel_fpr_bid / buy_fpr_bid | 0D FID41/51 | Exact item/route, positive prices, no crossed book |
| best ask/bid size | ka10004 sel_fpr_req / buy_fpr_req | 0D FID61/71 | Shares, required complete positive depth |
| clocks | REST response received time | Separate0B/0D receive times; optional0B FID20 trade time | No snapshot timestamp substitution, no common exchange sequence claimed |

REST symbol suffix definitions explicitly distinguish raw KRX, `_NX` and `_AL`. Static ka10001 fields, previous-day values, flows and minute bars are **not** replaced by current price. Postman includes query and JSON-body examples; unchanged client uses JSON body. ka10004 bid_req_base_tm is provenance-only; packaged description is not used to invent a fresh quote clock. No WS-derived minute bars are constructed from bounded tails.

## Implementation and review

- Existing atomic publisher adds process generation (PID/start ticks/boot ID), source commit/root, connection epoch, locally sent item registry, local cap and lock duration. Metadata/file I/O occurs outside the receiver lock. Registration state is explicitly not broker ACK; real0B/0D receipt remains separate.
- Reader is bounded to8MiB and one read per collector cycle. It checks original field ages <=20seconds (unchanged consumer limit), same date/session, exact item/route, positive complete fields, process lifetime, publisher/field epoch, and snapshot authority. File rewrite alone cannot renew old field clocks. Source/projection hashes, per-type sequences and optional provider trade-clock provenance are retained. Source absence has no recovery/network side effect.
- Samsung/Doosan/Hanwha keep existing REST prices/BBO and kernels. WS only adds `market_data_transport` comparison evidence. Different values are retained, never averaged or promoted; REST has no common exchange sequence, so even matched fields do not claim the same broker event. Actual source adoption and80% reduction/99% availability are not claimed.
- Per-process, exact consumer/session15-minute counters keep every active comparison including failed REST cycles; at most four windows are retained. Existing minute/state recorder retains the same transport annotation and DATA_WAIT failures. Existing paired replay rejects non-PASS source rows; missing prices/outcomes are not changed to zero. Comparison coverage is separate from adopted-input coverage and economics.
- Review covers producer freeze/atomic publication, standalone reader, stale/future/mixed routes, partial book, duplicate route, connection/producer generation, missing/invalid schema, failed REST denominator and unchanged collector requests. P1 does not use the Samsung display comparison or micro-reversion registration artifact as trading authority. Existing source subscriptions/locks/holdings and the separately approved25-share Samsung custody remain intact.

## Verification and handoff

- First targeted writer/reader/WS/three-collector run:426 tests PASS, including independent-process reader and unchanged REST request sets. Review fixes move cap-file access out of the WS lock, preserve failing comparison denominator, and expose exact rejection causes. Final validation/deployment/actual consumer evidence follow below.
- Existing owner remains `KiwoomCommonHealthOpportunityCostAcceptance0917`; no duplicate OPEN. P5 requires three consecutive15-minute windows and explained exact-route/field differences before P2. Missing/partial/mismatched comparison evidence keeps the current REST input and the source gap visible. No broad report regeneration, external sync, manual broker/provider probe or order is permitted by this diagnostic comparison.
- Final affected writer/reader/WS/three-collector/paired-replay suite:471 PASS; subsequently added exact NXT/integrated-route positives and reran the reader/quote suite:51 PASS. Ruff F/E9, Python compilation, diff whitespace and print-only checklist parser PASS. Existing fork-in-multithreaded-test DeprecationWarning is unchanged. Re-review has no unresolved in-scope P1 finding; P2/P3 and natural acceptance remain open.
- Rollback: restore the saved main release selection to `capacity-legacy-20260921-0d7fed630`, use the guarded restart, and remove only the three newly installed shared-WS P1 collector drop-ins before daemon-reload/restarting those collectors. Existing service drop-ins, trading owners, source history and daily generated data remain untouched. This is a rollback procedure, not an instruction to execute it now.

## Authorized deployment and natural receipt

- Source commit `87742bd0b710d2a6c240dfa34d5c319d75e1fadd` pushed to origin/main by ordinary fast-forward (including its existing local ancestors; no rewrite). Immutable release: `/home/ubuntu/KORStockScan-runtime-releases/widget-shared-ws-p1-20260921-87742bd0b`. Unrelated generated `data/threshold_cycle/date=2026-09-21/` is not staged or changed.
- Guarded main restart completed: PID131444, actual cwd in that release, exact-date bootstrap PASS at11:43:32 KST with zero missing/mismatched runtime policies. Natural scheduled error detection at11:44:45 reports7/7 PASS and no operational mutation. Health PASS is not P5 or economic acceptance.
- Three active collector-only services now use the same release via `zzzzzzzzzzzzzzzzzzzzzzz-shared-ws-p1-20260921.conf`: Samsung PID131725, Doosan PID131734, Hanwha Ocean PID131735; active/running, NRestarts0. Existing drop-ins remain intact. Widget signal auto-trader PID75952 and episode service were not restarted or repinned.
- Natural producer metadata carries main PID131444/start ticks1700364, epoch1, source commit87742bd0b and cap56. All three consumers recorded valid exact-route WS comparison input from that producer by11:44:24. First shown comparison: Samsung bid/ask quantities differ, Doosan ask quantity differs, Hanwha all five fields match. These are different receive times, **not** proof of same-event agreement or proof that all differences are explained. Their raw minute/state observation JSONL files also contain new producer hash and respective consumer PIDs at11:45:03–05.
- Startup old/stale checkpoint gaps and REST failures are retained in the denominator, not cleared after the first valid quote. Reader samples around12ms and a publisher lock sample138.813ms are isolated observations, not p95/p99 improvement claims. P0 matched baseline/resource distributions and natural3×15-minute acceptance remain open.
- Local rollback/evidence directory: `/home/ubuntu/KORStockScan/tmp/widget-shared-ws-deploy-20260921-wlJeKr` contains before/after main selection, previous service pins, installed drop-in sources, natural snapshots and scheduled health artifact. P5 next owner is the existing checklist item: inspect complete11:45–12:00,12:00–12:15,12:15–12:30 KST windows after12:30, retain all source gaps/differences and assess same-observation comparability before any P2 adoption. No WS source selection, wider subscriptions or P3 episode bars have been activated.
