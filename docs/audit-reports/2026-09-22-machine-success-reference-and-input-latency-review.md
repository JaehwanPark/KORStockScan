# 기계정책 성공 참조 평가와 입력 지연 보완 리뷰

작성: 2026-09-22 KST. Owner: 당일 체크리스트 `MainMachineSuccessReferenceEvaluation`.
요청: 기존 계획 §9 구현·리뷰·커밋/푸시·배포와 판정 전 기록 지연/최종 입력 로그 보완. AI 튜닝·체결/보유/청산 및 안전 임계치는 이 변경의 대상이 아니다.

## 변경과 리뷰

- 기계 단독 평가가 기존 ENTER_NOW 성공·실패·중립과 BLOCK/RECHECK를 동일한 비용 경로로 비교한다. 기회 내부 attempt 평균→기회 간 동일 가중을 유지하며, 개선값 `(candidate_enter-parent_enter)*net_path`의 분모는 공통 비교 모집단이다.
- 부모를 같은 순위 기준으로 평가한다. 순위는 표본 보정 승률→선택 순 경로 EV→공통 모집단 개선값→선택 기회 수→적은 분기/변경이다. 음수 EV도 허용하며 all-block은 순위에서 제외한다. 동일 정책은 재발행하지 않는다.
- 리뷰에서 원천 로더/공통 모집단이 비용 결손·미성숙 진입을 먼저 제거하는 누락을 발견하고 수정했다. 기계 단독 경로는 이 원천을 보호 검증용으로 유지하고 손익 점수에서는 제외한다. 평가되지 않은 기존 진입의 차단 전환은 거절한다. 신선도·hash·identity 원천 격리는 유지한다.
- 비용의 날짜/venue/session을 검증한다. 순 경로 0은 중립이며 소수 12자리 정규화로 부동소수점 잔차를 승리로 세지 않는다. 미래 입력/실제 AI/주문/PnL을 feature 또는 기계 경로 라벨에 넣지 않는다.
- v4 평가 basis·선정 버전·source/input/parent/scope를 evidence에 결속한다. 발행 gate에서 부모와의 모집단 불일치·기존 진입 변경 평가 누락·부모보다 낮은 순위를 거절한다. v3 발행 정책은 기존 계약으로 소비하며 v3 checkpoint를 새 점수로 재사용하지 않는다.
- main의 중복 prepromotion 문맥 기록은 이미 최종 입력을 저장하는 `mechanistic_entry_observation_v1` owner로 일원화한다. 기계 경로가 아닌 호출은 기존 기록을 유지한다. 최종 preflight와 trace는 같은 snapshot ID·시각·체결/호가 provenance를 사용한다.
- 정책 로더는 검증 중 실제 읽은 current receipt·generation·부모·근거/rollback 파일 전체의 서명을 추적한다. 모든 서명이 동일한 재조회는 검증 결과를 재사용한다. 파일 변경/누락·시각 유효성 변경은 재검증하며 반환 객체 변경은 캐시로 전파되지 않는다. 시장 freshness·hard guard는 별도 런타임 검사로 유지한다.
- 별도 evaluator/DB/cron/실시간 역사 검색/AI 호출을 추가하지 않았다. registry·96회 탐색 예산·tree 상한·3초 신선도·5초 처리 예산은 유지한다.

## 검증과 한계

- 관련 7개 테스트 owner: **747 passed, 1 deselected**. Python compile 및 `git diff --check` 통과. 추가 원천 보존·기존 실패만 회피한 후보·중립·모집단 고정·부모 동률·음수 EV·all-block·v3 호환·v4 혼합 거절·dependency cache 변경 검사를 포함한다.
- 제외 1건은 `test_holding_score_v2_payload_stays_compact_for_low_latency`: payload 5454자/기대 5000자. 변경 전 배포본 e66c7ed5a에서도 동일하게 실패한다. 본 변경과 분리된 기존 보유판정 테스트 결함이다.
- 동일 현행 정책 25회 조회: 최초 old/new 42.23/41.03ms(각 1회), 반복 중앙값 35.81/0.55ms, p95 37.62/0.57ms(각 24회). 실제 장중 지연이나 수익의 보증이 아닌 같은 호스트의 로더 측정이다.
- 최초 비교는 메모리/스왑 부담 때문에 9/21~9/22의 **사전 원천이 풍부한 상위 50종목**으로 고정했다. 원천완전 capture 수 내림차순/종목코드 동률 순이며 action·이후 손익은 선정에 사용하지 않았다. 995 capture 선택/467 이월, hash 등 원천 검사 후 880건(경로 평가 가능335/미성숙·비용 등 결손545). 이전 날짜는 이번 비교에서 이월하며 원본을 삭제하지 않는다. 같은 후보 목록/모집단으로 v3와 v4를 비교한다. 전체 시장 또는 전역 최적성 증명이 아니다.
- 마지막 체결 표시 시각→내부 수신 차이는 대한전선 0.197초, 090460 0.302초였다. 거래 시각 정밀도는 1초이고 provider 날짜도 수신일 기반이다. 확인된 것은 마지막 수신 이후 갱신 공백과 내부 준비 지연이다. 과거의 무체결/유실을 구분하는 연속성 근거는 없어 네트워크 장애나 정상 무체결로 확정하지 않는다.

## 원천 비교·배포 결과

고정 원천 비교 완료(세 scope 각96회 예산; 중복 등을 제외한 실제 평가84/82/84개). `training_through_date=2026-09-22`이며 독립 holdout 없음. 예정된 장후 호출이 이 인자를 생략해도 이전 명시 경계/발행 정책에서 학습에 사용한 날짜를 다시 독립 holdout으로 선언하지 않도록 승계 검증을 추가했다. 이 경계 승계 보완은 비교 후 추가했으며 점수·후보·판정 함수는 변경하지 않았다. 비교 당시 calibration 원본은 근거 디렉터리 `evaluated-calibration.py`에 보존한다.

| 정규장 동일 표본 | 부모 | v3 선정 후보를 v4로 평가 | v4 선정 |
| --- | ---: | ---: | ---: |
| 선택 기회 수 | 6 | 11 | 11 |
| 원 승률 | 50.00% | 54.55% | 54.55% |
| 보정 승률 점수 | 22.1260 | 31.5068 | 31.5068 |
| 평균 순 경로 EV | -0.4518417% | -0.3943955% | -0.3943955% |
| 공통 기회 분모 개선값 | 0 | -0.0017046%p | -0.0017046%p |
| 기존 성공 유지 | 3/3 | 3/3 | 3/3 |
| 기존 실패 유지/회피 | 3/0 | 3/0 | 3/0 |
| 추가 진입 성공/실패 | 0/0 | 3/2 | 3/2 |

정규장 비교 가능 원천은290attempt/136기회다. **이번 동일 후보 시험에서는 v3와 v4의 선정 결과가 같았다.** 성공 참조3건이 실제로 점수·유지율에 참여했음을 확인했으며, 이를 별도의 수익 개선 증명으로 표현하지 않는다. 승률 우선 순위상 후보는 부모보다 높지만 공통 분모 개선값은 음수이고 평균 EV도 음수다. 이는 사용자가 승인한 승률 우선/음수 허용 정책의 명시적인 절충이다.

정규장 후보는 기존3node 상한 내의 가격44350 분기이며 두 leaf 모두 `depth_adverse_ratio 2→3.75`, `absorption_buy_pressure 55→45`, `prior_adverse_drawdown_pct -1→-1.5`를 사용한다. 두 가격대의 임계치 차이가 생겼다는 의미는 아니다. root/unknown feature fallback은 부모를 보존한다. 통합시장(33attempt/20기회)은 부모 유지, 장전(4attempt/3기회)은 유효 선택 진입이 없어 부모 유지다.

같은 frozen 입력/정책의 이전·변경 코드 판정 결과 해시는 모두 일치했다. 정책별 warm400회, 최초 서로 다른 입력40회 측정에서 추가 파일 I/O는 없었다. 정규장 v3/v4 선정 정책의 p95는 이전 코드5.650/변경 코드5.635ms이고, 부모 정책은4.148/4.136ms다. 선택된3node 정책 자체는 기존1node 부모보다 약1.5ms 더 걸리며, 정책 로더 반복 조회 약35ms 절감과 별도로 보고한다. 최초40회 측정은 프로세스/OS cache 전체 초기화 실험이 아니다.

코드 리뷰의 미해결 범위 내 finding은0이다. 배포·정책 발행·PID 자연 소비는 아래 별도 영수증으로 닫는다.

근거 디렉터리: [`tmp/machine-success-v4-20260922`](../../tmp/machine-success-v4-20260922). `population-bound.json`, `frozen.json`, `comparison.json`, `candidate-policies.json`, `source-gap-diagnosis.json`, `tests-final.log`, `loader-before.json`, `loader-after.json`을 보존한다. 정책 비교는 비용 반영 가격 경로이며 실제 체결손익이 아니다.
