# Widget / episode AL completed-bar implementation review

## Observed-history follow-up review and repair

The user requested another review/fix cycle and authorized deployment/restart. The accepted observed-history policy remains in force. Review reproduced six failing cases in two defect groups: an excluded bar could poison receipt clocks/epochs (or raise a second exception), and `/proc` absence for an exited producer could be mistaken for missing native history, permitting REST fallback/seed. Receipt aggregation now uses exactly the accepted rows; invalid row epochs are excluded and any exclusion revokes the full-prefix claim. Dead/unreadable process identity becomes an explicit live-binding failure before the caller's missing-artifact handler. Original date/AL/hash/liveness and execution guards remain intact.

Live review also found quote source failures were replaced by a generic PID-binding message. The selector now preserves the original source-gap reason, without changing freshness thresholds or initiating recovery. Samsung's observed old0B/0D fields are distinguished from a PID mismatch. Quiet tape does not authorize reconnect/REG or historical-bar REST refresh. A genuine quote-time freshness failure can still block a current order decision; it is not a failed historical-bar cutover.

The initial six regressions failed before repair and passed after repair; supplemental tests cover full-prefix revocation and source-gap attribution. [Scoped test/review/deployment evidence](../../tmp/widget-p3-cutover-review-20260921/). Official upstream HEAD was rechecked unchanged at `953e5dbff123f437ab4d11a78a95191a685eb51f`; this change only repairs local receipt aggregation and error classification, with no request/FID/auth/subscription change. [Reference receipt](../../tmp/widget-p3-cutover-review-20260921/official-receipt.json). Deploy only the affected collector reader release and next-start episode definitions; preserve main/active episode PIDs and all state.

## Operator-approved observed-history cutover

The user explicitly accepted the observed degree of missing history and requested actual cutover validation. This supersedes the earlier complete-session-prefix and gap-free-history acceptance for the existing six-symbol cohort; earlier receipts below are historical. Identity/date/AL route, hash integrity, valid completed OHLCV, individual calculation minimums and execution-time hard guards remain mandatory. Three15-minute windows are follow-up quality observation, not a prerequisite to the first WS bar selection. No synthetic missing candle, adjusted REST/raw splice, order or custody change is authorized by this source switch.

`KORSTOCKSCAN_WS_COMPLETED_BAR_GAP_POLICY=observed_valid_rows` enables the approved behavior only within the existing explicit WS bar selectors/allowlists. Default remains strict; rollback sets this field to `strict` or restores the saved consumer drop-in. Normal completed rows before restart/gaps are retained with their original per-bar generation, timestamp and checkpoint hash. Gap/pending/invalid rows are excluded; receipt lists exclusions, observed range and `full_session_claimed=false`. A quiet snapshot age does not invalidate historical bars, while current producer liveness/connection/item binding and all quote/order-time guards remain separate. No publisher restart or producer rewrite is required.

Consumers label VWAP and range scope as observed valid history. Samsung's partial-session open/opening range remains unknown rather than substituting the first observed bar. Widget setup calculation can use the observed range; true time-specific trend/exit checks remain individual judgments. The symbol adapter uses policy minimum history plus the subsequent reclaim bar, not the maximum lookback-plus-setup retention span. Episode gateways propagate the observed-history basis to their four policy kernels; exact timestamp ordering and date identity remain required. No policy threshold/quantity values change.

Review corrections include passing the basis through gateway/parser/cache consumers, preserving source revalidation, excluding malformed OHLC rows individually, and keeping identity/hash/dead-producer failures blocking. Tests cover historical-row reuse after restart, missing minute acceptance, strict-mode isolation, partial-session labels, quiet extended-session data versus stale BBO, policy minimum plus reclaim, and gateway/policy use without REST. [Validation/rollout evidence](../../tmp/widget-p3-cutover-20260921/). Upstream HEAD was rechecked unchanged at `953e5dbff123f437ab4d11a78a95191a685eb51f`; relevant packaged ka10080/0B field semantics and realtime schema/spec loaders were inspected statically. [Reference receipt](../../tmp/widget-p3-cutover-20260921/official-receipt.json). No wire/FID/auth/subscription change or SDK execution is required.

**Cutover verification PASS at2026-09-21 19:02:59 KST.** Source `f6e7bf2a5751e7e4f649431eb1a2a4ad732aca1c` was committed/pushed to `codex/widget-p3-observed-cutover-20260921`, preserving current main source `ea21e9dcc` and the subsequent documentation consolidation parent `2e8a412f1`. Workspace and exact immutable release each passed **630 tests**, with one existing warning; compile and diff checks passed. The worktree has clean tracked source plus five intentional shared-directory symlinks. No producer modification or callback benchmark rerun was needed. [Validation](../../tmp/widget-p3-cutover-20260921/validation.json), [code push](../../tmp/widget-p3-cutover-20260921/push-receipt.json).

Four restarted collectors consumed the exact source and approved gap-policy env: Samsung PID378037, Doosan378025, HanwhaOcean378036, symbol378091. Main PID367246 and auto-expansion PID33200 are unchanged. Three ended Samsung episode definitions and the low-price template/auto-expansion definitions are bound for their next normal start; no ended machine was replayed. [Nine definition/rollback receipt](../../tmp/widget-p3-cutover-20260921/service-deployment.json), [actual four PID/env receipts](../../tmp/widget-p3-cutover-20260921/pid-consumption.json).

Three read-only samples from19:02:28 through19:02:59 validated all six AL sources. Final usable bars:005930=62,034020=75,042660=74,006800=73,010140=75,080220=73. All four active consumers (005930/006800/010140/080220) selected `kiwoom_ws_AL_completed_1m`, recorded `history_basis=observed_valid_rows` and per-bar-selection `rest_request_count=0`; final status/source-quality are `ok/PASS`. Consumer process receipts match the new PIDs. This proves actual live WS bar consumption, not merely a selector setting. [Timestamped cutover samples](../../tmp/widget-p3-cutover-20260921/live-cutover.json), [verification log](../../tmp/widget-p3-cutover-20260921/live-cutover.log).

One intermediate010140 cycle retained the existing quote receipt-binding guard and returned DATA_WAIT; the next cycle recovered to PASS naturally without restart/recovery forcing. This transient is preserved in the samples rather than reported as zero source waits. Doosan/Hanwha are outside their operating session: their source reads and installed PID/env passed, but no out-of-hours strategy evaluation was manufactured. The four episode adapters/policy kernels passed targeted tests; next normal episode execution remains distinct from today's live collector acceptance. [Latest captured process/resource health](../../tmp/widget-p3-cutover-20260921/health-receipt.json).

The approved first WS-bar cutover is closed. Three full15-minute quality windows, later episode natural evaluations and trade/economic results are follow-up observations, not unresolved blockers for this accepted cutover. Primary completed-bar reads above make zero REST requests; auxiliary/reference/account/order REST traffic is outside that statement. Execution-time freshness, quantity, account, duplicate-order, custody and hard protection remain intact. No manual order, external sync, broad report regeneration or main restart was performed.

Owner: `KiwoomCommonHealthOpportunityCostAcceptance0917`. Scope: [P3 connection contract](../proposals/widget-episode-shared-ws-market-data-improvement-plan-2026-09-21.md#331-p3-연결-확정-원천-상태분리와-단일-완료-분봉-발행). User authorized implementation, repeated review/fixes and deployment/restart. Source projection, consumer selection, natural acceptance and actual orders/economics are distinct.

## Implementation and review corrections

- Existing durable canonical journal writer owns the new `scalping/micro_reversion/completed_bars.py` projection. No extra WS client/service, callback file I/O, raw recapture or source sequence invention. Atomic per-date/AL-item/session checkpoints retain original clocks, observer epoch, explicitly bound transport epoch, process generation, source commit, revision/hash and durable journal byte cursor.
- First/reconnect partial minutes, missing raw FID13/15, local sequence gaps, AL cumulative/print inconsistencies, late revisions and actual capture loss are excluded. Watermark requires subsequent source event time plus the existing one-second allowance; wall time alone never finalizes a stopped stream. No-print minutes are absent, never synthetic OHLCV. KRX/NXT versus AL price/volume equality is not a promotion gate.
- Projection exceptions do not undo successful raw persistence. Optional projection configuration cannot stop raw capture. Publication happens on sealing, source/generation change or invalidation rather than every pending price change. Bounded checkpoint restore preserves sealed historical provenance and excludes the new partial minute; no full-session replay runs.
- Timestamp rejections are bounded by item, so a rejected print for another identifiable item does not poison this item's bars. Unknown source/worker/writer loss retains conservative generation barriers. Pre-existing historical loss is not reconstructed from the diagnostic tail.
- Common reader validates live producer/process, current observer-to-transport binding, exact date/AL/session, content hash, causal availability and OHLCV. Historical completed bars have no 20-second quote TTL. Live quote/order guards remain unchanged. Each read checks the current revision; collector/episode minute caches cannot hide a late invalidation.
- Existing five widget collectors and four episode gateways use the same adapter and their existing bar parser/signal kernels. REST and WS retain separate source/adjustment namespaces. The regular machine records actual bar provenance. Existing widget policy minimum history and regular episode lookback govern cutover; bootstrap/gap seed and raw WS histories are homogeneous, never spliced across an unverified adjustment boundary.
- Bootstrap/identified-gap `ka10080` requests use the existing client and admission policy. Filesystem `flock` shares one date/item/session/adjustment admission lease across processes, and each missing-range result is hash bound. Contenders return wait; failed/crashed attempts retain 60-second retry backoff. Original receive/availability clocks survive reuse. Quiet history itself requests no REST refresh; missing current order freshness still blocks through the existing guard.
- Same-generation quiet pre/aftermarket subscriptions do not repeat age-only persistent REG/REMOVE. Missing type/route, actual disconnect or new generation retains restoration. Successful REMOVE invalidates local sent-epoch/type receipts. Observation-only AL demotion cannot implicitly promote itself back into runtime ownership.
- Failure attribution clears previous-symbol request receipts and preserves controlled WS/bar failure reasons. Both 0B and 0D clock/epoch facts are reported separately. No age-based recovery permission is inferred from those facts.

## Official reference and validation evidence

Official repository revision: `953e5dbff123f437ab4d11a78a95191a685eb51f`, retrieved `2026-09-21T16:52:42.306079+09:00`. [Exact paths, hashes and retrieval receipt](../../tmp/widget-p3-impl-20260921/official-receipt.json): `kiwoom/_data/kiwoom_api_spec.json`, realtime packets/schemas, core WS/REST clients, specs and Postman. `kiwoom_docs` was absent at that revision. Inspected ka10080 one-minute/adjustment/item semantics, 0B FID10/13/15/20, 0D cumulative volume and REG/REMOVE retention semantics. No sample order was executed.

Tests include durable writer -> atomic file -> independent process reader, all four episode adapters with REST forbidden, widget adapter/provenance, initial/reconnect partial, quiet tape, same-minute invalidation, actual loss, duplicate/late events, checkpoint restart, isolated projection failure, another-symbol rejection isolation, process-shared seed contention/backoff and history-floor cutover. Callback preflight: [source-bound measured receipt](2026-09-21-widget-ws-completed-bars-latency-validation.json.txt), 15,000 measured callbacks with unchanged 1ms/2ms guards. [Validation command](../../tmp/widget-p3-impl-20260921/validate.sh).

Review found and corrected an old storage-maintenance pin dependency omitted from the new benchmark receipt; the prior reviewed hash was verified unchanged against selected parent `90dd032e3`, rather than widening the frozen runtime guard. The first synthetic projection was relocated to [isolated test evidence](../../tmp/widget-p3-impl-20260921/initial-synthetic-projection/); custom temporary journal roots now keep projections inside that root. Synthetic evidence is not natural market evidence.

## Operating rollout and rollback

| Setting | Role / default |
| --- | --- |
| `KORSTOCKSCAN_WS_COMPLETED_BARS_PUBLISH` | Optional source projection; launcher default `1`, explicit `0` disables it |
| `KORSTOCKSCAN_WS_COMPLETED_BAR_SYMBOLS` | Publisher allowlist: `005930,034020,042660,006800,010140,080220`; no additional registrations |
| `KORSTOCKSCAN_WIDGET_BAR_SOURCE` / `KORSTOCKSCAN_EPISODE_BAR_SOURCE` | Independent consumer source, default `rest`; `ws` requires a valid explicit allowlist |
| `KORSTOCKSCAN_WIDGET_BAR_WS_SYMBOLS` / `KORSTOCKSCAN_EPISODE_BAR_WS_SYMBOLS` | Exact six-digit consumer cohort; not inferred from registration or quote source |

Preserve the latest selected main release as parent, including concurrent entry-clock/input repairs. Use immutable source validation and selection compare-and-swap before any restart. Bind only the three basic collectors and existing symbol observer; the research universe and ended episodes are not restarted or replayed. First deploy publisher plus reader code; consumer selection requires exact natural source/required-lookback evidence. No order, custody, balance, provider, threshold, cap or source freshness relaxation is included.

Rollback consumer bar source to its prior `rest` setting independently of quote rollout. Publisher may be disabled without removing raw canonical journals. Release rollback must preserve successor changes; use the saved selection/drop-in receipts rather than resetting the workspace. Natural acceptance remains three full 15-minute windows within one producer generation/cohort/session, actual consumer receipts and measured primary bar REST count. Missing evidence, source gaps, real submissions and net-of-cost outcomes are not converted into PASS by unit tests.

## Earlier deployment and natural receipt

Main source publisher: `49e4448ac0ea4440d44d8609530c8de7cae15181`, PID `329859`, consumed at 17:28 KST. [Selection/PID receipt](../../tmp/widget-p3-impl-20260921/selection-after.json), [guarded restart and exact-date policy verification](../../tmp/widget-p3-impl-20260921/main-restart.log). Parent `90dd032e3` and its concurrent entry fixes are preserved. Policy verification passed with missing/mismatched values and unverified selected families all zero.

Final four-collector reader release: `c2ea0d267741468bf86eb1594f104b2cda9d9344`. Samsung PID336094, Doosan336097, Hanwha Ocean336093, symbol observer336164, all active/running with actual cwd verified. [Per-service deployment/backup receipts](../../tmp/widget-p3-impl-20260921/consumer-deployment.json). The immediate observer cwd check raced startup; the subsequent read-only check confirmed the stable PID, without an extra restart. Main remained PID329859 during this reader-only correction. The separate research service, widget trader and ended episode processes were not restarted.

Exact publisher release: **1,128 passed**. Final reader release: **1,129 passed**, two existing warnings. Targeted history correction:616 passed before immutable validation. Compile, Bash syntax, exact release diff, links, single OPEN owner and print-only backlog validation passed. The original canary storage pin failure was corrected by carrying the verified unchanged prior source dependency; no limit or baseline was relaxed. Callback p95 `0.027999ms`, p99 `0.047222ms`, drops/errors0.

Natural source evidence as of `2026-09-21T17:43:11.548243+09:00`: 005930/034020/042660/010140 each13 eligible completed bars;006800/080220 each12. All six independent reads validated, and each latest completed OHLCV matched its exact canonical raw sequence in a bounded2MiB tail. [Raw-to-bar evidence](../../tmp/widget-p3-impl-20260921/natural-receipt.json). Current observer/transport binding matched; writer loss, projection errors, configuration errors and new timestamp rejection counts were0. Startup partial minutes remain excluded. This is a source-quality result, not consumer input promotion or an order/economic result.

### Additional semantic finding: session anchors

Review of actual consumers found Samsung's session VWAP/opening range and006800/010140's `anchor_mode=session`. A mid-session WS suffix can contain many perfect candles while still replacing the opening anchor with the restart time. The final reader now distinguishes a complete session prefix from a rolling suffix. Session consumers cannot select the latter; a homogeneous REST seed retains an explicit `session_anchor_history_incomplete` blocker. Rolling symbol consumers require their existing full lookback plus setup-valid span, and regular episode history includes confirmation bars. No strategy window/threshold was relaxed.

**At the 17:43 review, consumer bar selectors remained `rest`; full P3 live cutover was not closed.** Next blocker owner is this record plus the original checklist ID: prove compatible initial session history/corporate-action price basis before combining adjusted REST and raw WS; then validate session VWAP/opening anchor equality and each rolling lookback before changing the exact cohort's selector. The inspected official REST specification and [official minute-chart example](https://github.com/Kiwoom-Securities/Kiwoom-REST-API/blob/953e5dbff123f437ab4d11a78a95191a685eb51f/examples/%EA%B5%AD%EB%82%B4%EC%A3%BC%EC%8B%9D/%EC%B0%A8%ED%8A%B8/get_domestic_stock_minute_chart.py) list adjustment0/1 without establishing this cross-source, same-day seed merge contract. Third-party interpretations were not used to authorize a merge. Waiting longer does not restore the missing session prefix.

The6-symbol producer remains running to accumulate natural evidence. Actual bar consumer cutover, three complete15-minute windows, primary-bar REST0 and source/submit/economic acceptance remain OPEN. No manual order/cancel, provider call, broad report regeneration or external Project/Calendar sync was performed by this work.

## Follow-up review and authorized commit/push rollout

The user explicitly authorized repeated review/fixes, commit/push and deployment/start. This review preserves the existing REST selectors and six-symbol publisher cohort. Session-prefix/price-basis integration remains the existing OPEN acceptance; it is not bypassed by this repair.

Confirmed and repaired three local contract defects:

1. Different missing ranges shared a process lock but separate failure clocks, allowing a second range to retry inside the first range's cooldown. A hash-bound date/item/session/adjustment admission receipt now shares the lock scope. Successful exact-range reuse still returns immediately with its original timestamp and zero new REST requests.
2. Missing hash, wrong JSON shape or incomplete seed receipts could escape the bootstrap handler as KeyError/TypeError, or an empty receipt could be mistaken for absence. Existing malformed receipts now produce controlled source failures and cannot trigger a replacement network call.
3. A caller with no seed callback could receive fewer WS bars than its declared floor; invalid history scopes could also reach bootstrap before validation. Floor/scope validation now precedes source lookup and insufficient history fails explicitly. Morning episodes inherit the regular machine's history-floor propagation; no duplicate morning implementation is needed.

Official upstream HEAD was rechecked unchanged at `953e5dbff123f437ab4d11a78a95191a685eb51f`; [fresh reference receipt](../../tmp/widget-p3-review-20260921/official-receipt.json). The local seed admission/cache repair does not change API payloads, FIDs, authentication or order calls. Targeted new regression coverage: cross-range failure cooldown/retry, malformed bootstrap caches, no-seed floor and invalid scope. [Follow-up validation and deployment directory](../../tmp/widget-p3-review-20260921/).

### Follow-up deployment receipt

Code release `d6b26e455b29b6e7b3a3d6fd6c06d722fd496bcf` was committed and pushed to `origin/codex/widget-p3-review-20260921`, preserving parent `c2ea0d267` and all selected main predecessors. The workspace's unrelated changes and main branch were preserved. Both workspace and immutable release validation passed **1,138 tests**, with two existing warnings. Python compile, Bash syntax, source hash checks, diff check, document links, one OPEN owner and print-only backlog passed. Source callback/journal bytes are unchanged from the prior measured latency receipt; a duplicate benchmark was unnecessary.

Deployment completed: main PID `343737`, Samsung `344071`, Doosan `344063`, Hanwha Ocean `344057`, symbol collector `344152`. All five actual cwd and source commit env match the immutable release, and the four services are active/running. Main exact-date policy/env verification passed with no missing or mismatched values. [Validation](../../tmp/widget-p3-review-20260921/validation.json), [PID receipts](../../tmp/widget-p3-review-20260921/pid-consumption.json), [collector rollback/drop-ins](../../tmp/widget-p3-review-20260921/collector-deployment.json), [guarded restart](../../tmp/widget-p3-review-20260921/main-restart.log).

At `2026-09-21T17:58:34.196918+09:00`, the shared WS producer belongs to the new main PID and all six AL source readers validate. Eligible post-gap completed bars: `{"005930": 1, "034020": 1, "042660": 1, "006800": 0, "010140": 0, "080220": 1}`. Writer loss, projection errors and configuration errors are zero. Old complete checkpoints remain historical; startup partial minutes are gaps and cannot be bridged into the eligible suffix. [Bounded natural receipt](../../tmp/widget-p3-review-20260921/natural-receipt.json). Bootstrap's prior-PID snapshot was rejected until the new connection published its own receipt; no stale handoff was promoted. A transient concurrent-publication mismatch was rechecked read-only and not used as valid source evidence.

This closes the reviewed cache/admission/floor defects and the approved commit/push/deployment/start. **Full P3 consumer cutover remains OPEN**: live bar selectors remain REST; compatible initial session history/price basis, actual WS bar consumption and three complete natural windows remain with the existing owner. No ended episode was replayed and no manual order/cancel, threshold, quantity, custody, provider or trading guard was changed.

## Native session history and readiness-driven consumption follow-up

The remaining work uses homogeneous AL WS history instead of assuming that undocumented REST adjustment0/1 is interchangeable with live prices. The current official [ka10080 portal](https://openapi.kiwoom.com/m/guide/apiguide/07/ka10080) still describes the field only as `0 or 1`; upstream HEAD remains `953e5dbff123f437ab4d11a78a95191a685eb51f`. [Reference/portal receipt](../../tmp/widget-p3-history-20260921/official-receipt.json). No REST wire field, FID mapping, quote mode or trading threshold was changed.

Reproduced structural blockers and repairs:

- Every first bar was marked partial and the reader required an actual opening-minute candle. Native session readiness could therefore remain permanently false, including a quiet opening with no trades. A first premarket print whose cumulative quantity equals its positive print quantity, or same-generation prior-session/boundary cumulative continuity, now establishes a hash-bound session-start receipt. No-print minutes remain absent.
- The previous checkpoint cursor could lag durable pending-minute prints. The new-session writer can inspect at most2MiB of the exact prior canonical journal after stat; the receipt records byte range, SHA and selected raw cursor. It does not scan/replay a whole day. A different PID/epoch/integrity, missing or malformed evidence, intervening volume, late/invalid print or truncated history cannot establish the prefix. Restored old checkpoints do not erase restart gaps.
- The producer bucket spans some times outside the reader's active bar session. Such boundary prints can advance the cumulative anchor/watermark but no out-of-session candle is emitted. Session bounds remain the established08:00–08:50,09:00–15:30 and16:00–20:00 source partitions; existing15:20 strategy/entry cutoff is unchanged.
- `KORSTOCKSCAN_WIDGET_BAR_SOURCE` / `KORSTOCKSCAN_EPISODE_BAR_SOURCE` now support explicit `ws_when_ready` alongside `rest` and strict `ws`. Within the existing six-symbol allowlist, sufficient valid native history selects WS automatically. Incomplete prefix/lookback retains the existing homogeneous REST path and minute cache, with a readiness reason attached to actual REST provenance. This mode does not freeze a one-time REST seed or add quiet-age refresh calls. Once a cache contains WS, every access validates its current revision; episode cache fallback cannot reuse a former WS result. Invalid live binding/hash/epoch fails explicitly. Quote source modes remain only rest/ws.

Review/fix loop caught and corrected initial placement of the new mode in the quote enum, then added an authority-boundary regression. It also added coverage revocation on epoch change, bounded tail provenance, truncation and closing-boundary tests. End-to-end fixtures prove native session selection with REST forbidden. [Validation and rollout evidence](../../tmp/widget-p3-history-20260921/). Measured15,000 callbacks retain original1ms/2ms guards, p95=0.030344ms and p99=0.099454ms, queue drops/worker errors0.

Deployment scope: preserve the newest main-strategy selected release as parent, restart main and four current collectors only, select `ws_when_ready` for their existing cohort, and bind the three ended Samsung episode services plus low-price template/auto-expansion definitions for their next normal start. No ended episode is replayed. The already-running auto-expansion process retains its current custody/source until its normal end; its installed next-start definition is distinct from PID consumption. No research universe or WS registration expansion, order/cancel, quantity, provider or threshold change is included. Consumer rollback is its saved drop-in/REST mode; publisher rollback preserves the latest main-strategy predecessor.

Historical mid-session source holes remain exclusions. Actual WS selection, three natural15-minute windows and reduced primary bar REST requests must be read from consumer receipts; code tests or the new selector value do not prove that those natural conditions have already occurred. This work removes structural readiness blockers and connects automatic selection; it does not invent the missing earlier session or force a same-day REST/raw merge.

### Native history deployment and live verification receipt

Code `fbe11c5f9633e6df4c12f014118878c55bba03dc` was committed and pushed to `origin/codex/widget-p3-native-history-20260921`. Its parent is the latest selected main follow-up `f40bbf482`; the running predecessor `77b6beb51` is also an ancestor. The selection compare-and-swap preserved concurrent main work and the workspace branch/index. [Code and validation receipt](../../tmp/widget-p3-history-20260921/validation.json), [push receipt](../../tmp/widget-p3-history-20260921/push-receipt.json).

Repeated review/fixes and validation passed **1,153 affected tests plus88 disjoint episode preflight/auto-expansion tests** in both workspace and exact immutable release, total1,241. Two pre-existing warnings remain. Python compile, Bash syntax, source hashes and diff checks passed. Final documentation receives the print-only parser, link and single-owner check; no external sync is run. No unresolved in-scope implementation finding remains. Natural acceptance below is separate from code review.

Main restart at18:29 KST consumed the new source as PID361275. Exact-date policy/env verification passed with missing/mismatched values0. Four restarted collectors consumed the same immutable source and `ws_when_ready` six-symbol cohort: Samsung361639, Doosan361627, HanwhaOcean361638, symbol361705. All four are active/running. [Actual PID/env receipt](../../tmp/widget-p3-history-20260921/pid-consumption.json), [guarded restart](../../tmp/widget-p3-history-20260921/main-restart.log), [nine definitions and rollback backups](../../tmp/widget-p3-history-20260921/service-deployment.json). The three ended Samsung services and low-price template remain inactive. Auto-expansion PID33200 retains its old source; only its next-start definition changed. No ended episode was replayed.

At18:35:23 KST all six readers accepted new PID361275/transport1. Eligible post-gap bars were005930=2,034020=3,042660=2,006800=3,010140=3,080220=3. Each latest completed OHLCV matched its exact durable raw sequence using a bounded2MiB tail. Writer loss, projection errors and configuration errors were0. Item-specific rejected prints remain explicit exclusions (including one005930_AL); they are not reported as zero. Historical complete checkpoints are not counted as the new generation's eligible suffix. [Natural raw-to-bar receipt](../../tmp/widget-p3-history-20260921/natural-receipt.json).

Live collector receipts confirm the new selection logic is running: Samsung/006800/010140 retain REST with `session_anchor_history_incomplete`;080220 retains REST with `completed_bar_history_insufficient` and existing50-bar floor. At18:35 Samsung and080220 reported `ok` and006800/010140 reported `data_wait`; the final18:36 consumer receipt records all four as `ok`, still using REST. A live service or health PASS does not turn those source waits into trading readiness. [Timestamped consumer, service and health receipt](../../tmp/widget-p3-history-20260921/final-runtime-receipt.json). Startup rejected the former PID's snapshot until the new producer published. Health was `pass` at18:34:58; the final18:36:04 receipt is `warning` because host I/O wait reached34.93% near the35% threshold. Process health remains `pass`. This host resource warning remains visible and is not code/source acceptance. No manual reconnect was forced for a quiet tape.

Implementation, reviewed source publication and the authorized restart are closed. **Actual native consumer cutover, same-generation3x15-minute acceptance, primary-bar REST0 and submission/economic outcomes remain OPEN** under `KiwoomCommonHealthOpportunityCostAcceptance0917`. Rolling consumers can switch when their current eligible history reaches the existing floor; session-anchor consumers need a proven complete native session from a subsequent boundary/start. Time alone cannot repair today's missing prefix. The next action is to read natural consumer selection receipts under those conditions, then validate the three windows and request counts. No threshold/quantity/custody or manual order/cancel was changed, and no broad trading/report regeneration was run.
