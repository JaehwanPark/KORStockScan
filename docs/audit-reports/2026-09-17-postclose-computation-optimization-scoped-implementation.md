# 장후 계산 최적화 부분 구현·리뷰 — 2026-09-17

## 1. 판정·범위

기존 [계산 계획](../proposals/postclose-computation-optimization-implementation-plan-2026-09-17.md)의 **O0 main wrapper·O3 감사 행 내부·O2 삼성 관측 feature 부분 수리**다. 전체 O0–O3/P1–P6 구현 완료가 아니다. 신규 모듈·collector·장후 작업·timer·TR/Provider 호출을 만들지 않았다. 다른 세션의 final-refresh wrapper/verifier/test/문서/generated 변경은 보존하고 이번 commit에서 제외한다.

## 2. 구현·반복 리뷰

- `run_threshold_cycle_postclose.sh`: 기존 process-group supervisor에서 명령별 wall·reaped child user/system CPU·largest waited child RSS·exit를 stderr `[PERF] postclose_command_metrics_v1`로 출력한다. 단순 `sleep`은 `bounded_wait`; 나머지 command의 내부 local/Provider/선행 대기는 아직 분리되지 않았다. stdout/status/경제성 report/hash에는 계측을 넣지 않는다. argv/payload/token은 출력하지 않는다. 정상/비정상 exit는 원래 값을 유지한다. HUP/INT/TERM은 원래 signal termination을 유지하며 부분 wall과 CPU/RSS null·`measurement_complete=false`를 남긴다. KILL 또는 supervisor 시작 전 종료의 완전 receipt는 보장하지 않는다. CPU는 shell/supervisor 사용량이 아니며 RSS는 동시 process-group 합계 peak가 아니다.
- `observation_source_quality_audit.py`: 각 행의 동일 정규화 결과를 계약 검사에 전달하여 재정규화를 제거한다. 원천·generation·이전 판정의 persistent cache를 쓰지 않는다. 반복 field-name source 분류만 pass-local 최대4,096개/label256자 bound로 재사용하고 값·의사결정은 저장하지 않는다. `_raw_generation`에 ctime을 추가해 동일 크기/mtime 복원 정정을 놓치지 않는다. JSON/gzip·유효/손상 행·원문 digest·행 제외·unknown/label/authority guard는 보존한다.
- `samsung_machine_entry_tuning.py`: cohort/window의 candidate-invariant drawdown/proximity 숫자를 한 번 변환한 뒤 기존 전체4군을 순서대로 평가한다. Matching·집계·순서·gate·정책 의미·owner state를 바꾸지 않는다. 현재 tightening subset-only 연구를 전수 미진입/완화 연구 완료로 표시하지 않는다.
- `scalping_avg_down_recovery_calibration.py`: 기존 replay source fingerprint의 size/mtime 뒤에 device/inode/ctime을 결속하여 같은 크기·mtime 복원 정정/원자 교체를 cache hit로 오인하지 않는다. 기존2필드 fingerprint는 새5필드와 불일치하여 한 번 miss/revalidation하며 과거 receipt를 새 검증으로 재라벨링하지 않는다. 정상 warm replay·Provider0 회귀와 실제 `_load_replay_cache`의 정정 시 결과 cache0을 검증했다. Provider 재시도·수량/추가매수/exit 권한을 바꾸지 않는다.

Supplemental review에서 spawn 실패 계측 누락, cache 위치/크기 bound, empty normalized mapping의 truthiness 오인, generation 동일 크기 정정, signal 부분 계측과 stderr 비공개성을 점검·회귀 보완했다. 검토 범위의 최종 finding0은 targeted 재검증 뒤 확정하며 전체 계획 finding0으로 전용하지 않는다.

## 3. 고정 fixture 성능·목적 부합성

등록183개 계약 ×3개 원천 변형 ×20반복 = **10,980행/2,527,440 bytes**. Fixture SHA256 `baad8b7b43dbe183f4e196a227af6b8c11b372c103fa6e3052b37d609588c57a`. 변형은 missing source·unknown/zero·integrated aftermarket axes다. Committed reference와 optimized를 같은 고정 in-memory JSON decode 원천으로 교대로5회 측정했다. 운영 raw/API/Provider를 읽지 않았다.

- Reference CPU초: 0.594407126, 0.624808520, 0.595821394, 0.621308951, 0.597064418.
- Optimized CPU초: 0.523170161, 0.509799198, 0.538800433, 0.516603978, 0.540112449.
- 중앙값0.597064418→0.523170161초, **12.376% 감소**, 전체 결과·행 제외·집계 동일. 마지막 label 길이 bound 보완 전 계측이므로 최종 managed 재측정은 별도 기록한다. Lifetime self peak RSS90,240KiB는 두 경로 독립 peak 비교가 아니며 RSS 개선을 입증하지 않는다.
- 감사의 계약 행 정규화2회→1회; 삼성 회귀 fixture의 관측 문자열 변환28회→7회. 후보 축소·sample/floor·holdout·경제성/비용 모델 변경 없이 중복 계산만 줄였다.

최종 release의 **계약명 정렬로 봉인한 별도 fixture**는 같은 행수/bytes이며 SHA256 `da71e0ddc93f5fc0bf4fe605d00a2ad0b166c3018ecd415ebb5467a92d69c113`이다. 초기 unsorted 계약 iteration fixture와 합산/교차 비교하지 않는다. 최종 label-length bound를 포함하고 managed module import를 확인했다. Reference CPU초 `[0.609637697,0.610918852,0.607378428,0.613136583,0.622512081]`, optimized `[0.520953290,0.510450110,0.542502740,0.510234277,0.542144525]`: 중앙값 **0.610918852→0.520953290초,14.726% 감소**. 전체 output parity와 digest `476f432f49357440ba31019a57fe620979840094ee597495f471e52a852140fe`를 확인했다. Memory-source decode/compute 비교이지 운영 disk read/critical-path benchmark가 아니다.

이는 작은 비용 후 수익을 빈번하게 만들기 위한 **계산 낭비 축소**이지 참여/실현순익 효과 증거가 아니다. 기계진입·AI보조·가격·수량/leg·scale-in·청산 및 hard/broker/custody/호가 guard를 변경하지 않았고 추가 live 승인·새 승격 floor를 만들지 않았다. 기존 자동 장후 producer는 해당 release 소비 시 계산 경로를 사용한다. 자연 실행·next-date selection/PID 소비·실체결 비용 후 EV는 별도다. O3 CPU30%/decode60%, O2 CPU50% 전체 목표를 달성했다고 주장하지 않는다.

## 4. 검증·릴리스

최초 감사/wrapper314건, 삼성/감사/wrapper/EV/router429건 후 ctime/cache 회귀 보완431건 통과. Signal/exit/stdout/CPU/wait 추가 targeted3건 통과. 최종 전체 재검증·source/import/hash·release routing은 아래에 추가한다.

매매 런타임 함수는 이번 변경 대상이 아니다. 기존 router가 main/start/postclose를 하나의 selected release로 관리하므로 selection/PID 계약을 닫는 데 필요한 main1회만 별도 승인된 안전 절차로 재기동했다. 독립 trader/collector/research unit은 유지한다. 변경 중인 장중 raw full scan·expensive 보고서·예정 전 service를 실행하지 않았다.

## 5. 미완료·다음 owner

기존 `KiwoomCommonHealthOpportunityCostAcceptance0917`을 유지한다. O0 machine wrapper/producer별 read bytes·row·cache·Provider/선행 wait 계측, 완료일 disk fixture·cold/warm/append/correction/cost/TERM 전체 matrix, O3 sealed-prefix projection/delta와 cumulative EV sufficient stats, O2 family checkpoint/grid 공통 state-aware replay, O1 exact request/예약·warm reuse 확장, P1–P6 scale acceptance는 **미완료**다. 이번 수리를 기존 source projection/전체 폐루프 완료로 합산하지 않는다. 다음 자연 장후 run의 phase receipt와 결과 parity를 기존 tower→checklist→strict→controller→finalization 체인으로 확인해야 한다.

## 6. 최종 검증·배포·실제 PID receipt

- Source/main `9c69864c`와 cache 보완 `2166c12c` push 완료. Managed `release/postclose-compute-20260917`의 `e64dae4ed37478be65c750140a095e3f0daed8d6` push 완료. Root `/home/ubuntu/KORStockScan-runtime-releases/postclose-compute-20260917`은 당시 실제 선택3261 baseline에 이번 source/test diff9파일만 적용했다. Main의 다른 연구 배포분/dirty 변경을 현재 거래 release에 임의 혼입하지 않았다. Independent research/evaluation pins의 기존 별도 릴리스를 보존한다.
- Workspace5 suite432 pass, AVG_DOWN3 suite106 pass와 추가 감사/cache260 pass. Managed final8 suite **531 passed/2 deselected**. 두 제외는 공유 `data` symlink 때문에 Git `check-ignore`가128을 반환하는 fixture이며 기능/경제성 테스트 제외가 아니다. Physical 경로의 해당2 fixture는 **2 passed**, shared 경로 복원 확인. 첫 physical5 suite427 pass, 첫 shared 경로425 pass/2 Git fixture fail을 숨기지 않고 분리했다. 최종 compile·shell·code-clean·diff 검증·4개 source/wrapper SHA 일치, print-only parser 확인. 검토된 부분 범위 finding0이며 전체 계획 closure가 아니다.
- Existing main selector를 기존 lock 아래 compare-before-atomic-move로 게시하고 router의 cron9 target·dated postclose/start print-plan을 확인했다. Selection backup `tmp/runtime_release_selection.pre-postclose-compute-20260917.json`, SHA `ebb089e169777e36c758d206f0d582aebb2e1582f538ca996d779362f5fb5f56`; 원본 physical mount backup `tmp/runtime_release_mounts/postclose-compute-20260917.ZIfe4w`는 보존했다.
- Main graceful1회: **12:38:35.088894 KST/PID384157**이 e64dae4e actual cwd/env/launcher receipt를 소비했고 source-dirty=false·singleton1·strict 당일 env/PID PASS(mismatch/missing0)다. **12:37:39.025287 사전→12:38:56.289945 사후** 양시장 broker 조회 complete, 삼성전자25주/매수가269471·미체결0으로 같았다. Owner registry `2975c91a…`, runtime env `65121195…`, 당일 machine policy `395a9447…` SHA 모두 같다. 주문 제출/취소·custody/env/정책 변경은 하지 않았다.
- 새 tmux session_created1789616313/current cwd가 새 root이며 bounded4000line/4MiB pane에서 로그인 응답 확인1·005160/043260 첫0B와010140/034020 등 첫0D 수신 marker를 확인했다. 과거 info/error 파일의 bounded tail에는 새 receipt가 없었으므로 그 파일을 최신 근거로 쓰지 않았다. 전scope quote freshness/실제 정책판정·새 submit/fill·순익 개선 증거는 아니다.
- Widget trader327012·episode auto-expansion327094와 collectors282239/282392는 원래 active/running PID/root를 유지한다. 20:10 widget evaluation·21:15 final refresh는 기존 admission release pin/MainPID0·not_yet_due이며 오늘 성공 terminal로 세지 않는다. Main 장후 producer는 아직 자연 실행 전이다. 오늘 장후 phase/consumer/strict 확인과 비용차감 economics는 OPEN이다.
- Scoped validation `data/runtime/runtime_release_validation/postclose-compute-20260917-e64dae4e.json`은 operating DONE·전수 구현 완료·경제성 승인 receipt가 아니다. Rollback도 현재 broker/owner 확인 후 기존 selector를 복구하고 main만 안전 재기동하는 범위이며 실행 중 wrapper/research pin을 교체하지 않는다.

## 7. O1 discovery-local floor 재파싱 보완

- `_historical_backfill_dates`→각 날짜 `_historical_backfill_floor`가 같은 historical floor JSON을 반복 decode하는 기존 경로를 대사했다. 기존 `ai_quality_cycle.py` 안에서만 discovery-local fact cache를 전달한다. Invocation마다 새 cache이며 최대32 entry/decoded entry256KiB다. No new module/collector/job/timer; checkpoint/Provider 결과·reservation/budget/state는 캐시하지 않는다.
- Pinned no-follow parent와 plain/gzip 양쪽의 device/inode/size/mtime/ctime census를 매번 확인한다. 변경·새 dual copy·상충·손상·삭제·symlink·부모 교체는 strict reader 재검증/원래 실패로 처리한다. Decode 도중 변경은 cached PASS가 되지 않는다. Writer lock busy이면 기다리지 않고 cache를 비운 뒤 원래 strict pinned reader로 fallback한다. Floor 의미 hash/날짜/기존 first-bound floor·source lineage·orphan/예산·Provider admission은 매 사용마다 원래 validator를 거친다.
- Private discovery/selection 경로에서 floor는 read-only 사실 자료다. 해당 consumers와 leaf validator의 비변경을 확인했다. 최초 방어적 deep-copy 안은 frozen fixture CPU79.63% 증가로 폐기했다. Mutable candidate/owner state를 공유하지 않으며 정규화 결과의 대조/hash 검증을 생략하지 않는다.
- Final frozen JSON53,805bytes SHA `094e10feb3a50c0c2060b2e3ba772ec24adbb688b2c4dae9aba2fc3d71467fd3`, 동일30회 read×교대5회. Reference CPU `[0.016753638,0.013910598,0.014493494,0.013942529,0.014309171]`, optimized `[0.004743998,0.004745768,0.004642915,0.004539572,0.004854461]`; median0.014309171→0.004743998초, **66.846% 감소**와 전체 output parity. 실제 discovery→2개 날짜 selection fixture strict decode3→1을 확인했다. 이는 작은 local floor read benchmark이며 전체 O1 CPU30% 목표/Provider transport·운영 critical path·RSS·순익 개선 근거가 아니다.
- Workspace 관련5 suite **430 passed**: cycle/runtime-family/standing-authorization/bridge/release-router. 추가 plain/gzip·수정·원자 교체·append·dual conflict·손상·symlink·missing·읽는 도중 변경·부모 교체·busy fallback·memory bound·별도 invocation·선정 oracle 회귀. Compile/diff check PASS; review finding0은 이 O1 부분 범위만이다. Live entry/AI 보조/가격/수량/leg/scale-in/exit·provider/model/cap/cost/floor는 변경하지 않았다.
- 전체 O0 내부 phase 계측/완료일 고정 fixture, O3 frozen projection·prefix proof/delta·EV 충분통계, O2 full-grid state-aware replay/append/정정/holdout, O1 전체 exact-request/aggregate reuse, P1–P6 scale matrix는 여전히 미완료다. 오늘 장후 자연 producer→consumer→strict 및 비용 차감 economics는 별도 OPEN이다. 이번 부분 범위의 배포·실제 PID 근거는 §8로 분리한다.

## 8. O1 부분 릴리스·장중 기동 검증

- Main source `02ca4dae`, managed source `f95a639f0d0265375cf3a971f5ab4d74d2c6d617` push 완료. Root `/home/ubuntu/KORStockScan-runtime-releases/postclose-floor-discovery-20260917`은 이전 실제 선택e64dae4e에 이번 Python/test **2파일만** 반영했다. Managed 관련5 suite430 PASS·compile/bash-n/source-clean·해당2파일 작업본/배포본 SHA 일치. Root의 source-dirty나 다른 세션의 final-refresh/verifier/문서 diff를 혼입하지 않았다.
- 기존 selector lock/compare-before-atomic-publish·cron9 target 및 당일 postclose/start print-plan 검증. Selector backup `tmp/runtime_release_selection.pre-postclose-floor-discovery-20260917.json` SHA `271b8480c4f8bc4578e6e79454899ea1a0a62d23d67f4e6dce34347a070ae1b5`, original physical mount backup `tmp/runtime_release_mounts/postclose-floor-discovery-20260917.5RoWxR`을 보존한다.
- 기존 main만 graceful1회: **12:52:42.460857 KST/PID403807**, selector `actual_pid_consumed=true`·actual cwd f95a root/src·singleton1·당일 strict env/PID PASS(mismatch/missing0). 장후 O1 함수의 자연 실행 receipt는 아니며 기존 장후 wrapper가 같은 release를 호출할 때 별도 확인한다.
- Broker **12:51:58.467131 사전→12:52:56.065804 사후**: KRX/NXT 둘 다 complete·삼성전자25주/매수가269471·미체결0 유지. Registry `2975c91a…`, 당일 runtime env `65121195…`, machine policy `395a9447…` SHA 모두 유지. 주문 제출/취소·수량/custody·env/policy 변경은 하지 않았다.
- 위젯 trader327012·episode expansion327094·collectors282239/282392 모두 active/running·기존 PID/root 유지. 20:10 evaluation/21:15 refresh의 기존 admission pin/MainPID0 유지, 조기 실행/당일 성공으로 재라벨링하지 않았다. 초기 pane login ACK1·first-data0 뒤 bounded 후속 pane에서 삼성005930 등0D·012330/034020/033640 등0B 자연 첫 수신·초기화 완료2·Traceback0을 확인했다. 전scope freshness/판정/실제 submit·fill·O1 장후 함수 소비/경제성 증거로 전용하지 않는다. 전체 계획/자연 장후/비용차감 economics OPEN.

## 9. O2 저가주 rolling feature·report 의존 결속

- 기존 `build_day_contexts`의 lookback15/20/30/45/60 전체 window slicing·연속성·극값 재계산을 monotonic deque와 마지막 시간 불연속 index로 대체했다. Window 시작 직전 gap은 허용하고 내부 gap/duplicate는 제외한다. Stable date/order·동일 극값 tie·positive 가격 검사·원래 부동소수점 연산 순서·원천 불변성을 유지한다. Stateful owner/candidate outcome cache는 공유하지 않는다. 새 module/collector/job·grid 축소·표본/floor/정책 변경이 없다.
- Expanded report fingerprint에 replay helper·실제 imported tick helper SHA, canonical cost contract 및 실제 replay cost 숫자를 결속한다. Helper/cost 변경은 report miss지만 비용 불변 source bar cache를 강제로 재수집하지 않는다. JSON list/null/scalar·손상 JSON은 reader miss로 보완했다. Persistent report body/native integrity 및 전체 checkpoint matrix 완료를 의미하지 않는다.
- Frozen old builder oracle: ordered/shuffled/gap/duplicate/zero/negative/short/empty, full grid·양 half·30 calibration/16 holdout의 zero/partial-fill/HELD 최종 선정·전체 DayContext/outcome-cache 상태 exact parity. Replay/tick/cost 변경 miss·non-object/corrupt6종 회귀. Workspace/managed5 suite **각355 PASS**, compile/bash-n/diff/source-clean 확인. 첫 suite 경로 오류로 exit4/no-tests를 확인한 뒤 실제 파일 경로로 재실행했다. 검토된 O2 부분 범위 finding0; 전체 계획 finding0이 아니다.
- Feature-only fixed8187 bars/966067bytes SHA `ada356e799efc02965e4f1456d25bd31d3bb2291758f489f140bfd22b1d6098e`: 교대5회 median CPU0.790331241→0.075700999초,90.422% 감소·전체 결과 parity. 별도 full construction+single-profile selection fixture8280 bars/977041bytes SHA `f18398b0fe92165960876d127347939f1f2d264d6232dde78e52ba1fd14f6d1b`, profile `samsung_heavy_midday`의 전체270 grid/46 source-day/holdout 선정: 교대3회 median CPU0.851817112→0.234662216초, **72.452% 감소**·전체 선정 output SHA `c62ba53e6dea07bcc317e61046d41cceeac1f08d87c34f2855e075dda96d23d8` 일치. 두 백분율을 합산하지 않는다. Synthetic calendar fixture이며 자연 시장/전체 family/운영 critical path·disk decode/RSS·수익 개선 증거가 아니다.
- 목적은 비용 후 소액 수익의 기회 분석을 후보 삭제 없이 빠르게 하는 것이다. 진입·AI보조·가격·수량/leg·scale-in·청산 owner 및 provider/model/cost/floor·호가/broker/hard/custody guard는 그대로다. 기존 main postclose의 expanded producer가 helper를 호출하고 final-refresh attribution은 결과 artifact를 소비한다. 독립 evaluation/refresh service는 helper를 직접 호출하지 않으므로 기존 pin을 유지한다. 자동 경로는 연결되어 있으나 오늘 자연 장후 함수 소비·정책 선정·경제성은 미관측이다.
- Main source `81464852`, managed source `5381cfdf10458a5e16ccafd3d74c0e61326dfa1b` 각각 push 완료. 이전 실제 selected f95a639f에 own4파일 source/test diff만 반영했다. Entry spot/helper test2파일 등3파일 SHA와 `reusable_report` AST 일치. Expanded producer는 작업본의 기존 admission projection과 managed baseline 차이를 보존했으므로 4파일 전체 SHA 동일로 주장하지 않는다. Foreign final-refresh/verifier/문서/generated 변경은 제외했다. 전체 O0 내부 phase/frozen 완료일 matrix·O3 sealed projection/prefix/delta/EV stats·O2 state-aware checkpoints/append/정정·O1 exact request/aggregate·P1–P6 scale acceptance는 OPEN이다.

## 10. O2 부분 릴리스·실제 장중 기동

- Selected root `/home/ubuntu/KORStockScan-runtime-releases/postclose-rolling-features-20260917`, managed commit `5381cfdf10458a5e16ccafd3d74c0e61326dfa1b`. 기존 selector lock 아래 이전 manifest 비교→atomic publish, cron9 target·dated postclose/start print-plan PASS. Router shell의 직접 실행은 execute bit 없어 exit126이었고 permission 변경 없이 기존 `bash deploy/run_runtime_release.sh`로 검증했다. Selector backup `tmp/runtime_release_selection.pre-postclose-rolling-features-20260917.json`, SHA `0d8b3eae441dd45a219e70b528da3f020c90af1f919bfe4944a262ea63ecccf6`; physical mount backup `tmp/runtime_release_mounts/postclose-rolling-features-20260917.fsUe0K` 보존.
- Main graceful1회 **13:33:37.394881 KST/PID456263**: selector `actual_pid_consumed=true`·cwd new root/src·singleton1·strict 당일 env/PID PASS(mismatch/missing0). **13:32:47.929760 사전→13:34:23.152181 사후** KRX/NXT broker complete·삼성전자25주/매수가269471·미체결0 동일. 이번 전후 registry `6fafbec5…`, runtime env `65121195…`, machine policy `395a9447…` hash 동일. 과거 registry `2975c91a…`를 이번 최신값으로 재사용하지 않았다. 주문 제출/취소·custody/env/policy 수정은 하지 않았다.
- Current pane64,619bytes bounded 확인: login ACK1·초기화 완료2·Traceback0·017670/387690 등 첫0B와005930/042660 등 첫0D. 이는 startup/수신 관측이지 전scope freshness·진입/체결/경제성 또는 장후 helper 자연 실행 receipt가 아니다. 독립4 service PID327012/327094/282239/282392와 기존 roots/pins 유지,20:10 evaluation·21:15 refresh MainPID0/not_yet_due 유지. 장후 expanded artifact→attribution→tower/checklist/strict/controller/finalization 및 비용차감 순익은 OPEN이다.

## 11. O2 calibration 관측창 clean-prefix 재생 통합

- 기존 후보마다 first-half/second-half/full calibration이 clean prefix를 각각 재생하는 경로를 기존 module의 private `_evaluate_candidate_windows`로 통합했다. Public `evaluate_candidate`는 같은 single-window API/결과를 유지한다. 한 후보의 prefix를 한 번 처리하며 각 관측창의 마지막 날짜에서 summary/episode snapshot을 봉인한다. HELD가 후반·holdout으로 이월되어 신규 entry를 차단하는 상태를 초기화하지 않고, 후기 보유 갱신을 전반 evidence로 역류시키지 않는다. Episode 포함 결과는 개별 deepcopy이며 candidate/invocation 간 state/result를 저장·공유하지 않는다. Requested union 밖의 episode/blocked-row를 보관하지 않는다.
- 같은 carry day의 저가는 두 leg마다 반복 scan하지 않고 한 번 계산한다. 종료 target을 다음 날짜 touch로 완료시키거나 HELD를 실현수익으로 바꾸지 않는다. Grid/순서/tie·전수 source-valid days/no-fill·partial/complete/held·cost/null·sample/holdout·선정/authority/owner 계약은 바꾸지 않는다. 새 file/module/collector/report/job·Provider/TR 요청이 없다. Persistent day checkpoint와 append/정정 invalidation은 이 통합에 포함되지 않는다.
- Supplemental review에서 일부 HELD 이름의 fixture가 profile 시간창이나 최소 drawdown을 충족하지 않아 무신호였음을 확인했다. Label을 coverage로 인정하지 않고 profile 창 직전 high→신호 뒤 target 미도달로 수정, 실제 `held_legs>0`·completed 발생 검사를 추가했다. 앞선 §9의 이름만 HELD인 fixture는 자연/실제 HELD acceptance가 아니다. New frozen serial oracle로 전체270 grid·중첩/비연속/역순/중복 관측창·서로 다른 last-date·빈 day·gap·mixed complete→held·미관측 clean-prefix carry를 대사했다. Include-episode mutable snapshot 분리·prebaseline prefix/invalid dates rejection, 최종30 calibration/16 holdout 선정 output 일치. Counted6일 fixture의 episode 평가15→6회. Workspace/managed5 suite **각376 PASS**, compile/ruff F,E9/bash-n/source-clean/own2파일 SHA·diff PASS. 검토된 O2 부분 범위 finding0이며 전체 계획이 아니다.
- Frozen8280 bars/977041bytes/46 synthetic calendar days·`samsung_heavy_midday` 전체270 grid, serial reference `d9a65805`의 evaluate/selection과 교대5회 CPU 비교. Builder는 공통 기존 optimized 경로로 timing 밖에서 실행한다. Zero SHA `634fa4bd9e4d1a7f2c909bc35aec7f4b3a5b46ff7ef065ece7d138529558db63`: median0.316402474→0.134673649초/**57.436% 감소**. Complete/partial SHA `de17e99b624fc529261d927b7af0eae3f0f7ee823eda59aae62a231840c0b2f4`: median0.163902197→0.087507653초/**46.610% 감소**. Actual-held SHA `bdcf6f2325e14454dbc9503145d6ebd3ce04854f98f327dbeacd011b2271d482`: baseline held1·blocked45일 확인, median0.170860359→0.078829663초/**53.863% 감소**. 초기 constant-high20100 HELD 측정은 무신호였으므로 HELD 효과 근거에서 제외했다. Whole output parity; actual-held output SHA `299f03457349da1bde4ed8b081b0a9f594c842f726bb625cac5009151c630e5d`.
- Tracemalloc selection Python allocation peak reference→optimized: zero59,688→62,368bytes, complete2,019,434→2,023,322bytes, actual-held75,922→75,928bytes. 작은 allocation 증가를 숨기거나 RSS 감소로 보고하지 않는다. 별도 process peak RSS/전체 family·N100·disk/critical path acceptance는 미완료다. §9 feature 효과와 합산하지 않으며 complete fixture의50% 목표 미달은 실제46.610%로 보고한다. 후보/표본/floor/비용/guard 완화로 목표를 맞추지 않는다.
- Main source `fbac8600`, managed `78eaceded83f20bc449ba8ef522a5433d986e489` push. 이전 selected5381에 own source/test2파일만 적용, 외부 dirty diff/pins 보존. 자동 main postclose expanded producer는 같은 helper/선정 경로를 호출하며 기존 replay-helper hash 결속으로 이전 report 재사용이 무효화된다. 승인·새 경제성 floor는 추가하지 않는다. 목적은 전체 미진입/진입 표본과 소액 비용후 수익 비교를 계산 낭비 없이 지속하는 것이며 실수익 개선 증명이 아니다. O0 내부 phase/완료일 matrix·O3 verified projection/prefix/delta/EV stats·O2 persistent replay checkpoints/append/정정·O1 exact request/aggregate reuse·P1–P6 scale/natural/경제성은 여전히 OPEN이다.

## 12. O2 관측창 재생 부분 릴리스·장중 기동

- Selected root `/home/ubuntu/KORStockScan-runtime-releases/postclose-custody-windows-20260917`, source `78eaceded83f20bc449ba8ef522a5433d986e489`·branch `release/postclose-custody-windows-20260917` push. 기존 selector lock/compare-before-atomic-publish·cron9 target 및 당일 postclose/start print-plan PASS. Backup `tmp/runtime_release_selection.pre-postclose-custody-windows-20260917.json` SHA `8017d1a794523211f1d6336dbd5d73c91d812ff04e61f6a2ac1724add1fb42d1`, physical mount backup `tmp/runtime_release_mounts/postclose-custody-windows-20260917.VUZv2w` 보존. Active root의 source를 제자리 수정하지 않았다.
- Main graceful1회 **13:41:33.411872 KST/PID467975**, source-dirty=false·actual cwd new root/src·singleton1·selector actual_pid_consumed=true·strict 당일 env/PID PASS(mismatch/missing0). Broker **13:41:00.011387 사전→13:42:17.882236 사후** KRX/NXT complete·삼성전자25주/매수가269471·미체결0 동일, registry `6fafbec5…`·runtime env `65121195…`·machine policy `395a9447…` SHA 동일. 수동 주문 제출/취소·정책/env/custody 변경은 하지 않았다.
- 초기 bounded pane49,019bytes에서 login ACK1·초기화2·first-data0·Traceback0, 후속55,035bytes pane에서 삼성005930/432470 등의 자연 첫0B와010140/017670 등의 첫0D 수신·Traceback0 확인. 초기 수신 전 상태를 장애 또는 전체 freshness 성공으로 오판하지 않는다. 독립4 service PID327012/327094/282239/282392 active/running 유지·기존 release roots/pins 유지. 20:10 evaluation/21:15 refresh MainPID0/not_yet_due 유지.
- Validation `data/runtime/runtime_release_validation/postclose-custody-windows-20260917-78eacede.json`은 scoped code/release/PID 관측이며 전수 구현/운영 terminal/경제성 승인 artifact가 아니다. 자동 장후 helper 자연 호출→expanded artifact→attribution→tower/checklist/strict/controller/finalization과 비용차감 순익은 아직 별도 OPEN이다. 기존 owner `KiwoomCommonHealthOpportunityCostAcceptance0917`을 닫지 않는다.

## 13. O2 report 재사용 무결성·후행 전달 보완

- Persistent checkpoint 확장 전 기존 `reusable_report`가 입력 fingerprint만 맞으면 손상된 본문이나 임의 status를 재사용하는 결손을 확인했다. 기존 expanded producer의 `write_report`가 `low_price_two_leg_verified_report_cache_v1` receipt/body SHA를 원자적 JSON 발행에 포함하도록 보완했다. Digest는 receipt 자신을 제외한 실제 본문 전체를 canonical JSON으로 결속하며 nonfinite 값은 허용하지 않는다. 호출자 객체는 변경하지 않으며 알림 결과 저장 시 새 본문에 재발급한다. 신규 module/collector/report producer/cron은 없다.
- Reader는 exact target/input·현행 schema·정상 terminal3종과 source-only authority, 기존 `CandidateRecommendationNotifier._valid_report`의 native inventory/count/cost/선정 계약을 검증한다. Digest 재봉인만으로 의미 위반을 통과시키지 않는다. 즉시 부모/leaf symlink·FIFO·읽는 중 inode/size/mtime/ctime 변경과 parent 교체를 거절하고 descriptor를 닫는다. Shared release의 정상 data ancestor symlink는 허용한다. 파생 report reader bound128MiB는 cache miss guard이며 원천 차단/표본 감소/경제성 조건이 아니다. 관측된 전일 report21,728,331bytes는 stat만 확인했으며 운영 본문/raw를 전수 재처리하지 않았다.
- 구 unsealed receipt·corrupt/NaN/nonobject·비정상 terminal은 full recompute 경로로 돌아가지만 비용 불변 source bar cache를 강제 재수집하지 않는다. 정상 partial-source 격리/무추천 terminal은 별도 실체결·양수EV gate 없이 재사용한다. Owner 정책·floor·holdout·grid·cost·provider/model·수량/leg·진입/AI보조·scale-in·exit/broker/hard/custody guard는 유지한다. 목적은 전체 미진입/진입 기회와 비용후 소액 수익 평가를 손상 결과로 대체하지 않고 지속하는 것이다. 새 수익·운영 critical path 개선 수치의 증명은 아니다.
- 추가 리뷰에서 warm hit가 `--notify`를 건너뛰는 결손을 찾아 기존 notifier의 dedup/bounded retry를 호출하고 현재 telegram 결과를 재봉인하도록 보완했다. 알림 실패는 fresh 경로와 동일하게 RuntimeError/nonzero이고, sent/duplicate 및 기존 sent_state_persist_failed 의미는 유지한다. 재계산 생략을 후행 전달 생략으로 바꾸지 않는다. 테스트에서는 notify/TR/replay를 mock하고 실제 Telegram/Provider/주문을 실행하지 않았다.
- 본문 변조5종·재봉인 의미 위반11종·불안정/unsafe reader5종·구 receipt/NaN·정상 partial-source 격리·발행 중단 시 이전 결과 보존/임시파일 정리·warm notify 미요청/성공/duplicate/실패를 검증했다. Expanded67 PASS; 작업본8 suite372 PASS, managed 공통7 suite328 PASS physical/shared 각각. 작업본의 admission-only 추가 suite44건은 managed baseline에 없는 코드를 배포한 증거가 아니다. Cache writer/digest/reader AST와 test SHA parity·compile·ruff F,E9·bash-n·diff PASS. 테스트 경로 오류(no-tests/exit4)를 수정한 뒤 위 실제 suite를 재실행했다. 검토한 2파일 범위 finding0이며 전체 계획 finding0이 아니다.
- Main `6d6e4bc9`, managed `e47587c998c5799e4b61685b2a517b0d24accf78` 각각 push. Managed78eacede baseline에 이번 own2파일 변경만 반영했다. Workspace admission projection의 기존 차이는 보존했으며 producer 전체 SHA 동일로 주장하지 않는다. Persistent candidate/day checkpoint·append/정정/partition/full scale matrix, O0 내부 phase/완료일 matrix·O3 verified projection/prefix/delta/EV stats·O1 exact request/aggregate·P1–P6 전수 acceptance는 미완료다.

## 14. 검증된 cache 부분 릴리스·기존 매매 PID 유지

- Selected root `/home/ubuntu/KORStockScan-runtime-releases/postclose-verified-cache-20260917`, managed source `e47587c998c5799e4b61685b2a517b0d24accf78`/branch `release/postclose-verified-cache-20260917` push. Source-clean·shared paths 확인 후 기존 selector lock 아래 이전 manifest 비교/atomic publish, cron9 target·당일 postclose/start print-plan PASS. Backup `tmp/runtime_release_selection.pre-postclose-verified-cache-20260917.json` SHA `6fe6f6d96e8caccc86cec47c89a82cc0b6158e8299a289f41d62e03cb21cd5bc`, mount backup `tmp/runtime_release_mounts/postclose-verified-cache-20260917.i9sEPf` 보존. 최초 read-only active-wrapper 점검은 검사 명령 자신의 문자열을 오인해 publish 전 차단됐으며, `/proc`의 개별 argv basename으로 재확인 후 실행했다. 이전 root를 제자리 수정하지 않았다.
- 수정 함수는 기존 main postclose expanded producer의 별도 process에서 호출되고 attribution/auto-expansion/intake는 artifact를 소비한다. 기존 자동 실행에 연결되어 별도 승인·수동 env·새 job이 필요하지 않다. 오늘 장후 실행은 아직 도래 전이며 early producer/비싼 report 재생성은 하지 않았다. 기존 bots의 코드·거래 동작 변경이 없어 graceful 재기동은 하지 않았다. Main PID467975/cwd78eacede root 유지·singleton1·strict 당일 env/PID PASS(mismatch/missing0), 새 selector의 `actual_pid_consumed=false`를 그대로 기록했다. Start print-plan은 기동 가능 경로 점검이지 새 PID 소비 또는 매매 활성화가 아니다.
- Broker13:51:50.519251 사전→13:54:35.623915 사후 KRX/NXT complete·삼성25주/매수가269471·미체결0 동일, registry `6fafbec5…`·runtime env `65121195…`·dated machine policy `395a9447…` SHA 동일. 주문 제출/취소·custody/env/policy 변경은 하지 않았다. 독립4 service PID327012/327094/282239/282392 active/running·기존 root 유지,20:10 evaluation/21:15 refresh의 admission root pin·MainPID0/not_yet_due 유지했다.
- Validation `data/runtime/runtime_release_validation/postclose-verified-cache-20260917-e47587c9.json`은 scoped code/selected-release 관측이다. 새 main PID consumption·자연 cache hit/알림 후행·artifact→tower/checklist/strict/controller/finalization·정책 선정/비용차감 실현수익은 별도 미관측이며 운영 완료/GREEN으로 보고하지 않는다. 기존 OPEN owner를 유지한다.
